local defaults = {}

defaults.separator = " "
defaults.gveq = true

-- Window options
defaults.window_width = 30
defaults.window_height = 15
defaults.window_opacity = 80
defaults.window_style = 'minimal'
defaults.window_border = 'double'
defaults.window_relative = 'cursor'

return defaults
