local defaults = {}

defaults.separator = " "
defaults.gveq = true

-- Window options
defaults.window_width = 30
defaults.window_height = 15
defaults.window_opacity = 30
defaults.window_style = 'minimal'
defaults.window_border = 'single'
defaults.window_relative = 'cursor'
defaults.window_col = 0
defaults.window_row = 1

return defaults
